v_add_f32 v0, v1, v2        // VOP2: v0 = v1 + v2 (two vector sources)
    s_add_u32 s0, s1, 16       // SOP2: scalar add with small encoded immediate
    v_mov_b32 v3, 0x3f800000   // VOP1 with literal: assembler emits literal following base // literal = 1.0f